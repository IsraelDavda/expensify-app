

// 1 challange 1
// take input and output revrse
//soltion 1 take the argum string split it to array do on this revers & on this join to join the array
// function out(str){
//str.split('').reverse().join('')}
/////////////
// soltion 2 do varible to empty & do for loop that i eQual to str.length -1 (coz index 0 ) - >= 0 i--
//& return varible + the varible +str[i] ( so varible empty and everytime its add to first latter to first)
// let tempvar = ''; for(i = str.length-1; i >=0; i--){return tempvar + tampvar +str[i]}
/////////////////
// challange 2 do palindrome
// soltion 1 do const var do str split revrese & return var === str return true or false
// // const check = (str) => {
//     const revStr = str.split('').reverse().join('')
//     return revStr === str
// }////////////////
// challange 3 get int and eturn revrse it
//soltion 1 do let to take the int do it to string &split it revrse it join and return the let praseint to number
///////
//1.reverse() do revrseto the array
// 2. split('') without space split str to individual array
/// 3. join('') join the array space toghter to string
/// 4. praseInt() take string to int
//5 toString() become a string
//6 indexOf() count the position but its index 0 so u can add 1 to real result
//7.
//8.